# Evals

Use this directory to compare model behavior across providers.

First eval ideas:

- Same paper, different model: are required paper-card fields complete?
- Same topic synthesis, different model: are citations traceable?
- Same concept tagging task, different model: are tags stable?
- Same related-work draft, different model: does it preserve uncertainty?


# Uncertainty Policy

Use these labels:

- `confidence: high`: directly supported by source material.
- `confidence: medium`: supported by source material but requires interpretation.
- `confidence: low`: plausible but weakly supported.

Use `needs_review: true` when a human should confirm the note before it is treated as reliable project memory.


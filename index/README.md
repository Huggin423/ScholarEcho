# Index

This directory is for generated search and retrieval artifacts.

Expected future files:

- `metadata.sqlite`: structured metadata for papers, concepts, methods, and projects.
- `embeddings/`: vector index files.
- `graph.generated.json`: generated concept and citation graph.

Do not treat generated index files as the source of truth. The source of truth is the Markdown knowledge layer.

